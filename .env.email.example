# ===== Configurações de Email (SMTP) =====
# Necessário para recuperação de senha

# Gmail (recomendado)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app  # Senha de app do Gmail (não a senha normal)
FROM_EMAIL=seu-email@gmail.com
FROM_NAME=EXPO TV

# URL do frontend (onde está o formulário de reset)
FRONTEND_URL=https://expotv.com.br

# Ambiente (development ou production)
ENV=production

# ===== Como configurar Gmail =====
# 1. Acesse: https://myaccount.google.com/apppasswords
# 2. Crie uma "Senha de app" para "Mail"
# 3. Cole a senha gerada em SMTP_PASSWORD
# 
# ⚠️ IMPORTANTE: Nunca commite este arquivo com senhas reais!

# ===== Outros provedores SMTP =====

# SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=sua-api-key-sendgrid

# Mailgun
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@seu-dominio.mailgun.org
# SMTP_PASSWORD=sua-senha-mailgun

# Amazon SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=sua-access-key
# SMTP_PASSWORD=seu-secret-key
