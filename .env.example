# ========================================
# 🗄️ BANCO DE DADOS
# ========================================

DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_HOST=localhost
DB_PORT=3306
DB_NAME=expotv

# URL completa (alternativa - use DB_* acima)
DATABASE_URL=mysql+pymysql://user:password@localhost/database_name


# ========================================
# 📧 EMAIL (SMTP) - GMAIL
# ========================================

# Configurações do servidor SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Credenciais de email
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app-do-gmail

# Email remetente
FROM_EMAIL=seu-email@gmail.com
FROM_NAME=EXPO TV


# ========================================
# 🔐 AUTENTICAÇÃO
# ========================================

# Chave secreta para JWT (gerar com: python -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=sua-chave-secreta-super-segura-mude-em-producao-2025

# Tempo de expiração do token (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=43200  # 30 dias


# ========================================
# 🌐 APLICAÇÃO
# ========================================

# URL do frontend (para redirecionamento após reset)
FRONTEND_URL=https://seu-frontend.com.br

# URL do backend (para página de reset de senha)
BACKEND_URL=https://expotv-backend.fly.dev

# Ambiente (development ou production)
ENV=production


# ========================================
# ☁️ CLOUDFLARE R2 (Storage)
# ========================================

# Configurações do R2
R2_ACCOUNT_ID=seu_account_id
R2_ACCESS_KEY_ID=sua_access_key
R2_SECRET_ACCESS_KEY=sua_secret_key
R2_BUCKET_NAME=ged
R2_PUBLIC_URL=https://pub-xxx.r2.dev


# ========================================
# 🚀 SERVIDOR
# ========================================

# Porta do servidor (padrão: 8000)
PORT=8000

# Host do servidor
HOST=0.0.0.0


# ========================================
# 📝 INSTRUÇÕES
# ========================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Preencha as credenciais reais em .env
#
# 3. NUNCA commite o arquivo .env com credenciais reais!
#
# 4. Para Gmail:
#    - Use "senha de app" (não a senha normal)
#    - Criar em: https://myaccount.google.com/apppasswords
#    - Requer verificação em 2 etapas ativa
#
# 5. Para gerar SECRET_KEY segura:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"

